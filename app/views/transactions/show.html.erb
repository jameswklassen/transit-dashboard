<h1>Transaction <%= @transaction.transaction_number %></h1>

<%= react_component("Transaction", { transaction: @transaction }) %>

<% @transaction.attributes.each do |key, value| %>
  <div>
    <%= "#{key}: #{value}"%>
  </div>
<% end %>
